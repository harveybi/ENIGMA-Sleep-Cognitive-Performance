# Data Directory

This directory intentionally contains only non-human external reference data.

- `external/natural_earth/`: map source data used by manuscript plotting scripts.

Controlled-access cohort data must not be committed here. Approved users should configure local paths in `configs/paths.local.yml`.
